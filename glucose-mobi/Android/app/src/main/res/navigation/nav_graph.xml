<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@id/recordFrag">

    <fragment
        android:id="@+id/recordFrag"
        android:name="rl.collab.aiglucose.frag.RecordFrag"
        android:label="@string/record"
        tools:layout="@layout/frag_record" />

    <fragment
        android:id="@+id/figureFrag"
        android:name="rl.collab.aiglucose.frag.FigureFrag"
        android:label="@string/figure"
        tools:layout="@layout/frag_figure" />

    <fragment
        android:id="@+id/accFrag"
        android:name="rl.collab.aiglucose.frag.AccFrag"
        android:label="@string/acc"
        tools:layout="@layout/frag_acc" />
</navigation>